[pytest]
markers =
    stress: Cenários de stress/carga leve para o backend (podem demorar mais).

filterwarnings =
    ignore::DeprecationWarning:httpx._client
    ignore::DeprecationWarning:httpx._content
    ignore::DeprecationWarning:jose.jwt

